{% extends 'base.html.twig' %}

{% block title %}Edit Recept{% endblock %}

{% block body %}
    <h1>Edit Recept</h1>

    {{ include('recept/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('recept_index') }}">back to list</a>

    {{ include('recept/_delete_form.html.twig') }}
{% endblock %}
